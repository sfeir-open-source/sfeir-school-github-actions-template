name: npm test
on: 
  workflow_dispatch:
               
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: setup-node-version
        uses: actions/setup-node@v3
        with:
          node-version: lts/*
      - name: install-npm-action
        run: npm install
